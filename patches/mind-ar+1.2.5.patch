diff --git a/node_modules/mind-ar/dist/controller-mGt1s8dJ.js b/node_modules/mind-ar/dist/controller-mGt1s8dJ.js
index 2c757fa..0bcdeda 100644
--- a/node_modules/mind-ar/dist/controller-mGt1s8dJ.js
+++ b/node_modules/mind-ar/dist/controller-mGt1s8dJ.js
@@ -55171,8 +55171,22 @@ class FY {
     const o = ps().runWebGLProgram(t, e, s);
     return Ot().makeTensorFromDataId(o.dataId, o.shape, o.dtype);
   }
+  dispose() {
+    try {
+      if (this.tempPixelHandle) {
+        const e = ps();
+        e.disposeData(this.tempPixelHandle.dataId), this.tempPixelHandle = null;
+      }
+    } catch (e) {
+    }
+    try {
+      this.context && this.context.canvas && (this.context.canvas.width = 0, this.context.canvas.height = 0);
+    } catch (e) {
+    }
+    this.program = null, this.context = null;
+  }
 }
-const rd = { memory: wl, nextFrame: su }, VY = 1e-3, zY = 1e3, PY = 5, AY = 5;
+const rd = { memory: wl, nextFrame: su }, VY = 1e-3, zY = 1e3, PY = 5, AY = 5, TRACK_EVERY_N_FRAMES = 2;
 class QQ {
   constructor({
     inputWidth: t,
@@ -55224,9 +55238,22 @@ class QQ {
     }), this.markerDimensions = i, { dimensions: i, matchingDataList: r, trackingDataList: o };
   }
   dispose() {
-    this.stopProcessVideo(), this.worker.postMessage({
-      type: "dispose"
-    });
+    if (this._disposed)
+      return;
+    this._disposed = !0, this.stopProcessVideo();
+    try {
+      this.worker && this.worker.postMessage && this.worker.postMessage({ type: "dispose" });
+    } catch (t) {
+    }
+    try {
+      this.worker && this.worker.terminate && this.worker.terminate();
+    } catch (t) {
+    }
+    this.worker = null;
+    try {
+      this.inputLoader && this.inputLoader.dispose && this.inputLoader.dispose();
+    } catch (t) {
+    }
   }
   // warm up gpu - build kernels is slow
   dummyRun(t) {
@@ -55280,37 +55307,96 @@ class QQ {
         trackMiss: 0,
         filter: new WC({ minCutOff: this.filterMinCF, beta: this.filterBeta })
       });
+    let e = 0;
     (async () => {
-      for (; this.processingVideo; ) {
-        const s = this.inputLoader.loadInput(t);
-        if (this.trackingStates.reduce((r, i) => r + (i.isTracking ? 1 : 0), 0) < this.maxTrack) {
-          const r = [];
-          for (let l = 0; l < this.trackingStates.length; l++)
-            this.trackingStates[l].isTracking !== !0 && (this.interestedTargetIndex !== -1 && this.interestedTargetIndex !== l || r.push(l));
-          const { targetIndex: i, modelViewTransform: a } = await this._detectAndMatch(s, r);
-          i !== -1 && (this.trackingStates[i].isTracking = !0, this.trackingStates[i].currentModelViewTransform = a);
+      for (; ; ) {
+        if (!this.processingVideo)
+          break;
+        const s = this.trackingStates.reduce((l, u) => l + (u.isTracking ? 1 : 0), 0) < this.maxTrack, o = !s && this.trackingStates.some((l) => l.showing && l.isTracking) && this.trackingStates.every((l) => !l.isTracking || l.showing && l.isTracking);
+        let r = !0;
+        if (o && TRACK_EVERY_N_FRAMES > 1)
+          r = e % TRACK_EVERY_N_FRAMES == 0, e += 1;
+        else
+          e = 0;
+        if (!r) {
+          for (let l = 0; l < this.trackingStates.length; l++) {
+            const u = this.trackingStates[l];
+            if (!u.showing || !u.trackingMatrix)
+              continue;
+            let d = [];
+            for (let h = 0; h < u.trackingMatrix.length; h++)
+              d[h] = u.trackingMatrix[h];
+            t.width === this.inputHeight && t.height === this.inputWidth && (d = this.getRotatedZ90Matrix(d)), this.onUpdate && this.onUpdate({ type: "updateMatrix", targetIndex: l, worldMatrix: d });
+          }
+          if (!this.processingVideo)
+            break;
+          if (this.onUpdate && this.onUpdate({ type: "processDone" }), !this.processingVideo)
+            break;
+          await rd.nextFrame();
+          continue;
         }
-        for (let r = 0; r < this.trackingStates.length; r++) {
-          const i = this.trackingStates[r];
-          if (i.isTracking) {
-            let a = await this._trackAndUpdate(s, i.currentModelViewTransform, r);
-            a === null ? i.isTracking = !1 : i.currentModelViewTransform = a;
+        let i = null;
+        try {
+          if (i = this.inputLoader.loadInput(t), !this.processingVideo)
+            break;
+          if (this.trackingStates.reduce((l, u) => l + (u.isTracking ? 1 : 0), 0) < this.maxTrack) {
+            const l = [];
+            for (let d = 0; d < this.trackingStates.length; d++)
+              this.trackingStates[d].isTracking !== !0 && (this.interestedTargetIndex !== -1 && this.interestedTargetIndex !== d || l.push(d));
+            const { targetIndex: u, modelViewTransform: d } = await this._detectAndMatch(i, l);
+            if (!this.processingVideo)
+              break;
+            u !== -1 && (this.trackingStates[u].isTracking = !0, this.trackingStates[u].currentModelViewTransform = d);
+          }
+          for (let l = 0; l < this.trackingStates.length; l++) {
+            if (!this.processingVideo)
+              break;
+            const u = this.trackingStates[l];
+            if (u.isTracking) {
+              let d = await this._trackAndUpdate(i, u.currentModelViewTransform, l);
+              if (!this.processingVideo)
+                break;
+              d === null ? u.isTracking = !1 : u.currentModelViewTransform = d;
+            }
+            if (u.showing || u.isTracking && (u.trackMiss = 0, u.trackCount += 1, u.trackCount > this.warmupTolerance && (u.showing = !0, u.trackingMatrix = null, u.filter.reset())), u.showing && (u.isTracking ? u.trackMiss = 0 : (u.trackCount = 0, u.trackMiss += 1, u.trackMiss > this.missTolerance && (u.showing = !1, u.trackingMatrix = null, this.onUpdate && this.onUpdate({ type: "updateMatrix", targetIndex: l, worldMatrix: null })))), u.showing) {
+              const d = this._glModelViewMatrix(u.currentModelViewTransform, l);
+              u.trackingMatrix = u.filter.filter(Date.now(), d);
+              let h = [];
+              for (let p = 0; p < u.trackingMatrix.length; p++)
+                h[p] = u.trackingMatrix[p];
+              t.width === this.inputHeight && t.height === this.inputWidth && (h = this.getRotatedZ90Matrix(h)), this.onUpdate && this.onUpdate({ type: "updateMatrix", targetIndex: l, worldMatrix: h });
+            }
           }
-          if (i.showing || i.isTracking && (i.trackMiss = 0, i.trackCount += 1, i.trackCount > this.warmupTolerance && (i.showing = !0, i.trackingMatrix = null, i.filter.reset())), i.showing && (i.isTracking ? i.trackMiss = 0 : (i.trackCount = 0, i.trackMiss += 1, i.trackMiss > this.missTolerance && (i.showing = !1, i.trackingMatrix = null, this.onUpdate && this.onUpdate({ type: "updateMatrix", targetIndex: r, worldMatrix: null })))), i.showing) {
-            const a = this._glModelViewMatrix(i.currentModelViewTransform, r);
-            i.trackingMatrix = i.filter.filter(Date.now(), a);
-            let l = [];
-            for (let u = 0; u < i.trackingMatrix.length; u++)
-              l[u] = i.trackingMatrix[u];
-            t.width === this.inputHeight && t.height === this.inputWidth && (l = this.getRotatedZ90Matrix(l)), this.onUpdate && this.onUpdate({ type: "updateMatrix", targetIndex: r, worldMatrix: l });
+          if (!this.processingVideo)
+            break;
+          this.onUpdate && this.onUpdate({ type: "processDone" });
+        } finally {
+          try {
+            i && i.dispose && i.dispose();
+          } catch (l) {
           }
+          i = null;
         }
-        s.dispose(), this.onUpdate && this.onUpdate({ type: "processDone" }), await rd.nextFrame();
+        if (!this.processingVideo)
+          break;
+        await rd.nextFrame();
       }
     })();
   }
   stopProcessVideo() {
     this.processingVideo = !1;
+    const t = this.workerMatchDone, e = this.workerTrackDone;
+    this.workerMatchDone = null, this.workerTrackDone = null;
+    if (t)
+      try {
+        t({ targetIndex: -1, modelViewTransform: null, debugExtra: null });
+      } catch (s) {
+      }
+    if (e)
+      try {
+        e({ modelViewTransform: null });
+      } catch (s) {
+      }
   }
   async detect(t) {
     const e = this.inputLoader.loadInput(t), { featurePoints: s, debugExtra: o } = await this.cropDetector.detect(e);
@@ -55328,19 +55414,28 @@ class QQ {
     return e.worldCoords.length < 4 ? null : await this._workerTrackUpdate(t, e);
   }
   _workerMatch(t, e) {
-    return new Promise(async (s, o) => {
+    return new Promise((s) => {
       this.workerMatchDone = (r) => {
-        s({ targetIndex: r.targetIndex, modelViewTransform: r.modelViewTransform, debugExtra: r.debugExtra });
-      }, this.worker.postMessage({ type: "match", featurePoints: t, targetIndexes: e });
+        this.workerMatchDone = null, s({ targetIndex: r.targetIndex, modelViewTransform: r.modelViewTransform, debugExtra: r.debugExtra });
+      };
+      try {
+        this.worker.postMessage({ type: "match", featurePoints: t, targetIndexes: e });
+      } catch (r) {
+        this.workerMatchDone = null, s({ targetIndex: -1, modelViewTransform: null, debugExtra: null });
+      }
     });
   }
   _workerTrackUpdate(t, e) {
-    return new Promise(async (s, o) => {
+    return new Promise((s) => {
       this.workerTrackDone = (a) => {
-        s(a.modelViewTransform);
+        this.workerTrackDone = null, s(a.modelViewTransform);
       };
-      const { worldCoords: r, screenCoords: i } = e;
-      this.worker.postMessage({ type: "trackUpdate", modelViewTransform: t, worldCoords: r, screenCoords: i });
+      try {
+        const { worldCoords: r, screenCoords: i } = e;
+        this.worker.postMessage({ type: "trackUpdate", modelViewTransform: t, worldCoords: r, screenCoords: i });
+      } catch (a) {
+        this.workerTrackDone = null, s(null);
+      }
     });
   }
   _glModelViewMatrix(t, e) {
diff --git a/node_modules/mind-ar/dist/mindar-image-three.prod.js b/node_modules/mind-ar/dist/mindar-image-three.prod.js
index c19cd8e..45e6224 100644
--- a/node_modules/mind-ar/dist/mindar-image-three.prod.js
+++ b/node_modules/mind-ar/dist/mindar-image-three.prod.js
@@ -13580,15 +13580,39 @@ class Zb {
     userDeviceId: c = null,
     environmentDeviceId: d = null
   }) {
-    this.container = e, this.imageTargetSrc = t, this.maxTrack = a, this.filterMinCF = o, this.filterBeta = l, this.warmupTolerance = p, this.missTolerance = m, this.ui = new Rd({ uiLoading: r, uiScanning: n, uiError: u }), this.userDeviceId = c, this.environmentDeviceId = d, this.shouldFaceUser = !1, this.scene = new Ct(), this.cssScene = new Ct(), this.renderer = new Ti({ antialias: !0, alpha: !0 }), this.cssRenderer = new Vd({ antialias: !0 }), this.renderer.outputEncoding = Si, this.renderer.setPixelRatio(window.devicePixelRatio), this.camera = new vi(), this.anchors = [], this.renderer.domElement.style.position = "absolute", this.cssRenderer.domElement.style.position = "absolute", this.container.appendChild(this.renderer.domElement), this.container.appendChild(this.cssRenderer.domElement), window.addEventListener("resize", this.resize.bind(this));
+    this.container = e, this.imageTargetSrc = t, this.maxTrack = a, this.filterMinCF = o, this.filterBeta = l, this.warmupTolerance = p, this.missTolerance = m, this.ui = new Rd({ uiLoading: r, uiScanning: n, uiError: u }), this.userDeviceId = c, this.environmentDeviceId = d, this.shouldFaceUser = !1, this.scene = new Ct(), this.cssScene = new Ct(), this.renderer = new Ti({ antialias: !0, alpha: !0 }), this.cssRenderer = new Vd({ antialias: !0 }), this.renderer.outputEncoding = Si, this.renderer.setPixelRatio(window.devicePixelRatio), this.camera = new vi(), this.anchors = [], this.renderer.domElement.style.position = "absolute", this.cssRenderer.domElement.style.position = "absolute", this.container.appendChild(this.renderer.domElement), this.container.appendChild(this.cssRenderer.domElement), this._resizeHandler=this.resize.bind(this),window.addEventListener("resize",this._resizeHandler);
   }
   async start() {
     this.ui.showLoading(), await this._startVideo(), await this._startAR();
   }
   stop() {
-    this.controller.stopProcessVideo(), this.video.srcObject.getTracks().forEach(function(t) {
-      t.stop();
-    }), this.video.remove();
+    try {
+      this._resizeHandler && (window.removeEventListener("resize", this._resizeHandler), this._resizeHandler = null);
+    } catch (e) {
+    }
+    try {
+      this.controller && (typeof this.controller.dispose == "function" ? this.controller.dispose() : this.controller.stopProcessVideo && this.controller.stopProcessVideo());
+    } catch (e) {
+    }
+    try {
+      const e = this.video && this.video.srcObject;
+      const t = e && typeof e.getTracks == "function" ? e.getTracks() : [];
+      const a = new Set();
+      for (let n = 0; n < t.length; n++) {
+        const r = t[n];
+        if (!r || a.has(r)) continue;
+        a.add(r);
+        try {
+          r.readyState !== "ended" && typeof r.stop == "function" && r.stop();
+        } catch (i) {
+        }
+      }
+    } catch (e) {
+    }
+    try {
+      this.video && typeof this.video.remove == "function" && this.video.remove();
+    } catch (e) {
+    }
   }
   switchCamera() {
     this.shouldFaceUser = !this.shouldFaceUser, this.stop(), this.start();
diff --git a/node_modules/mind-ar/src/image-target/controller.js b/node_modules/mind-ar/src/image-target/controller.js
index 2dd3d9f..7385adb 100644
--- a/node_modules/mind-ar/src/image-target/controller.js
+++ b/node_modules/mind-ar/src/image-target/controller.js
@@ -13,6 +13,11 @@ const DEFAULT_FILTER_BETA = 1000;
 const DEFAULT_WARMUP_TOLERANCE = 5;
 const DEFAULT_MISS_TOLERANCE = 5;
 
+// Patch D-lite: while a target is stably showing+tracking, run the expensive
+// loadInput/detect/track/worker path only once every N loop iterations.
+// Skipped iterations hold the last valid pose (re-emit) and still await nextFrame.
+const TRACK_EVERY_N_FRAMES = 2;
+
 class Controller {
   constructor({inputWidth, inputHeight, onUpdate=null, debugMode=false, maxTrack=1, 
     warmupTolerance=null, missTolerance=null, filterMinCF=null, filterBeta=null}) {
@@ -112,10 +117,26 @@ class Controller {
   }
 
   dispose() {
+    if (this._disposed) return;
+    this._disposed = true;
+    // Soft-stop first so in-flight awaits can resolve and dispose inputT.
     this.stopProcessVideo();
-    this.worker.postMessage({
-      type: "dispose"
-    });
+    try {
+      this.worker && this.worker.postMessage && this.worker.postMessage({ type: "dispose" });
+    } catch (err) {
+      // ignore
+    }
+    try {
+      this.worker && this.worker.terminate && this.worker.terminate();
+    } catch (err) {
+      // ignore
+    }
+    this.worker = null;
+    try {
+      this.inputLoader && this.inputLoader.dispose && this.inputLoader.dispose();
+    } catch (err) {
+      // ignore
+    }
   }
 
   // warm up gpu - build kernels is slow
@@ -179,99 +200,156 @@ class Controller {
       //console.log("filterMinCF", this.filterMinCF, this.filterBeta);
     }
 
+    // Counts only stable (showing+tracking) iterations for D-lite cadence.
+    let stableFrameIndex = 0;
+
     const startProcessing = async() => {
       while (true) {
 	if (!this.processingVideo) break;
 
-	const inputT = this.inputLoader.loadInput(input);
-
-	const nTracking = this.trackingStates.reduce((acc, s) => {
+	const nTrackingPreview = this.trackingStates.reduce((acc, s) => {
 	  return acc + (!!s.isTracking? 1: 0);
 	}, 0);
+	const needsDetect = nTrackingPreview < this.maxTrack;
+	const stableShowing =
+	  !needsDetect &&
+	  this.trackingStates.some((s) => s.showing && s.isTracking) &&
+	  this.trackingStates.every((s) => !s.isTracking || (s.showing && s.isTracking));
+
+	let runHeavyPipeline = true;
+	if (stableShowing && TRACK_EVERY_N_FRAMES > 1) {
+	  runHeavyPipeline = (stableFrameIndex % TRACK_EVERY_N_FRAMES) === 0;
+	  stableFrameIndex += 1;
+	} else {
+	  stableFrameIndex = 0;
+	}
 
-	// detect and match only if less then maxTrack
-	if (nTracking < this.maxTrack) {
-
-	  const matchingIndexes = [];
+	// D-lite skip: no drawImage / GPU upload / track / worker — hold last pose.
+	if (!runHeavyPipeline) {
 	  for (let i = 0; i < this.trackingStates.length; i++) {
 	    const trackingState = this.trackingStates[i];
-	    if (trackingState.isTracking === true) continue;
-	    if (this.interestedTargetIndex !== -1 && this.interestedTargetIndex !== i) continue;
-
-	    matchingIndexes.push(i);
+	    if (!trackingState.showing || !trackingState.trackingMatrix) continue;
+	    let clone = [];
+	    for (let j = 0; j < trackingState.trackingMatrix.length; j++) {
+	      clone[j] = trackingState.trackingMatrix[j];
+	    }
+	    const isInputRotated = input.width === this.inputHeight && input.height === this.inputWidth;
+	    if (isInputRotated) {
+	      clone = this.getRotatedZ90Matrix(clone);
+	    }
+	    this.onUpdate && this.onUpdate({type: 'updateMatrix', targetIndex: i, worldMatrix: clone});
 	  }
+	  if (!this.processingVideo) break;
+	  this.onUpdate && this.onUpdate({type: 'processDone'});
+	  if (!this.processingVideo) break;
+	  await tf.nextFrame();
+	  continue;
+	}
 
-	  const {targetIndex: matchedTargetIndex, modelViewTransform} = await this._detectAndMatch(inputT, matchingIndexes);
+	let inputT = null;
+	try {
+	  inputT = this.inputLoader.loadInput(input);
+	  if (!this.processingVideo) break;
 
-	  if (matchedTargetIndex !== -1) {
-	    this.trackingStates[matchedTargetIndex].isTracking = true;
-	    this.trackingStates[matchedTargetIndex].currentModelViewTransform = modelViewTransform;
-	  }
-	}
+	  const nTracking = this.trackingStates.reduce((acc, s) => {
+	    return acc + (!!s.isTracking? 1: 0);
+	  }, 0);
+
+	  // detect and match only if less then maxTrack
+	  if (nTracking < this.maxTrack) {
 
-	// tracking update
-	for (let i = 0; i < this.trackingStates.length; i++) {
-	  const trackingState = this.trackingStates[i];
+	    const matchingIndexes = [];
+	    for (let i = 0; i < this.trackingStates.length; i++) {
+	      const trackingState = this.trackingStates[i];
+	      if (trackingState.isTracking === true) continue;
+	      if (this.interestedTargetIndex !== -1 && this.interestedTargetIndex !== i) continue;
 
-	  if (trackingState.isTracking) {
-	    let modelViewTransform = await this._trackAndUpdate(inputT, trackingState.currentModelViewTransform, i);
-	    if (modelViewTransform === null) {
-	      trackingState.isTracking = false;
-	    } else {
-	      trackingState.currentModelViewTransform = modelViewTransform;
+	      matchingIndexes.push(i);
+	    }
+
+	    const {targetIndex: matchedTargetIndex, modelViewTransform} = await this._detectAndMatch(inputT, matchingIndexes);
+	    if (!this.processingVideo) break;
+
+	    if (matchedTargetIndex !== -1) {
+	      this.trackingStates[matchedTargetIndex].isTracking = true;
+	      this.trackingStates[matchedTargetIndex].currentModelViewTransform = modelViewTransform;
 	    }
 	  }
 
-	  // if not showing, then show it once it reaches warmup number of frames
-	  if (!trackingState.showing) {
+	  // tracking update
+	  for (let i = 0; i < this.trackingStates.length; i++) {
+	    if (!this.processingVideo) break;
+	    const trackingState = this.trackingStates[i];
+
 	    if (trackingState.isTracking) {
-	      trackingState.trackMiss = 0;
-	      trackingState.trackCount += 1;
-	      if (trackingState.trackCount > this.warmupTolerance) {
-		trackingState.showing = true;
-		trackingState.trackingMatrix = null;
-		trackingState.filter.reset();
+	      let modelViewTransform = await this._trackAndUpdate(inputT, trackingState.currentModelViewTransform, i);
+	      if (!this.processingVideo) break;
+	      if (modelViewTransform === null) {
+		trackingState.isTracking = false;
+	      } else {
+		trackingState.currentModelViewTransform = modelViewTransform;
 	      }
 	    }
-	  }
 
-	  // if showing, then count miss, and hide it when reaches tolerance
-	  if (trackingState.showing) {
-	    if (!trackingState.isTracking) {
-	      trackingState.trackCount = 0;
-	      trackingState.trackMiss += 1;
+	    // if not showing, then show it once it reaches warmup number of frames
+	    if (!trackingState.showing) {
+	      if (trackingState.isTracking) {
+		trackingState.trackMiss = 0;
+		trackingState.trackCount += 1;
+		if (trackingState.trackCount > this.warmupTolerance) {
+		  trackingState.showing = true;
+		  trackingState.trackingMatrix = null;
+		  trackingState.filter.reset();
+		}
+	      }
+	    }
 	    
-	      if (trackingState.trackMiss > this.missTolerance) {
-		trackingState.showing = false;
-		trackingState.trackingMatrix = null;
-		this.onUpdate && this.onUpdate({type: 'updateMatrix', targetIndex: i, worldMatrix: null});
+	    // if showing, then count miss, and hide it when reaches tolerance
+	    if (trackingState.showing) {
+	      if (!trackingState.isTracking) {
+		trackingState.trackCount = 0;
+		trackingState.trackMiss += 1;
+
+		if (trackingState.trackMiss > this.missTolerance) {
+		  trackingState.showing = false;
+		  trackingState.trackingMatrix = null;
+		  this.onUpdate && this.onUpdate({type: 'updateMatrix', targetIndex: i, worldMatrix: null});
+		}
+	      } else {
+		trackingState.trackMiss = 0;
 	      }
-	    } else {
-	      trackingState.trackMiss = 0;
 	    }
-	  }
 	    
-	  // if showing, then call onUpdate, with world matrix
-	  if (trackingState.showing) {
-	    const worldMatrix = this._glModelViewMatrix(trackingState.currentModelViewTransform, i);
-	    trackingState.trackingMatrix = trackingState.filter.filter(Date.now(), worldMatrix);
+	    // if showing, then call onUpdate, with world matrix
+	    if (trackingState.showing) {
+	      const worldMatrix = this._glModelViewMatrix(trackingState.currentModelViewTransform, i);
+	      trackingState.trackingMatrix = trackingState.filter.filter(Date.now(), worldMatrix);
 
-	    let clone = [];
-	    for (let j = 0; j < trackingState.trackingMatrix.length; j++) {
-	      clone[j] = trackingState.trackingMatrix[j];
-	    }
+	      let clone = [];
+	      for (let j = 0; j < trackingState.trackingMatrix.length; j++) {
+		clone[j] = trackingState.trackingMatrix[j];
+	      }
 
-      const isInputRotated = input.width === this.inputHeight && input.height === this.inputWidth;
-      if (isInputRotated) {
-        clone = this.getRotatedZ90Matrix(clone);
-      }
+	      const isInputRotated = input.width === this.inputHeight && input.height === this.inputWidth;
+	      if (isInputRotated) {
+		clone = this.getRotatedZ90Matrix(clone);
+	      }
 
-	    this.onUpdate && this.onUpdate({type: 'updateMatrix', targetIndex: i, worldMatrix: clone});
+	      this.onUpdate && this.onUpdate({type: 'updateMatrix', targetIndex: i, worldMatrix: clone});
+	    }
 	  }
-	}
 
-	inputT.dispose();
-        this.onUpdate && this.onUpdate({type: 'processDone'});
+	  if (!this.processingVideo) break;
+	  this.onUpdate && this.onUpdate({type: 'processDone'});
+	} finally {
+	  try {
+	    inputT && inputT.dispose && inputT.dispose();
+	  } catch (err) {
+	    // ignore
+	  }
+	  inputT = null;
+	}
+	if (!this.processingVideo) break;
 	await tf.nextFrame();
       }
     }
@@ -280,6 +358,25 @@ class Controller {
 
   stopProcessVideo() {
     this.processingVideo = false;
+    // Resolve in-flight worker waits so the loop can exit and finally-dispose inputT.
+    const matchDone = this.workerMatchDone;
+    const trackDone = this.workerTrackDone;
+    this.workerMatchDone = null;
+    this.workerTrackDone = null;
+    if (matchDone) {
+      try {
+	matchDone({ targetIndex: -1, modelViewTransform: null, debugExtra: null });
+      } catch (err) {
+	// ignore
+      }
+    }
+    if (trackDone) {
+      try {
+	trackDone({ modelViewTransform: null });
+      } catch (err) {
+	// ignore
+      }
+    }
   }
 
   async detect(input) {
@@ -308,21 +405,33 @@ class Controller {
   }
 
   _workerMatch(featurePoints, targetIndexes) {
-    return new Promise(async (resolve, reject) => {
+    return new Promise((resolve) => {
       this.workerMatchDone = (data) => {
+        this.workerMatchDone = null;
         resolve({targetIndex: data.targetIndex, modelViewTransform: data.modelViewTransform, debugExtra: data.debugExtra});
       }
-      this.worker.postMessage({type: 'match', featurePoints: featurePoints, targetIndexes});
+      try {
+        this.worker.postMessage({type: 'match', featurePoints: featurePoints, targetIndexes});
+      } catch (err) {
+        this.workerMatchDone = null;
+        resolve({targetIndex: -1, modelViewTransform: null, debugExtra: null});
+      }
     });
   }
 
   _workerTrackUpdate(modelViewTransform, trackingFeatures) {
-    return new Promise(async (resolve, reject) => {
+    return new Promise((resolve) => {
       this.workerTrackDone = (data) => {
+        this.workerTrackDone = null;
         resolve(data.modelViewTransform);
       }
-      const {worldCoords, screenCoords} = trackingFeatures;
-      this.worker.postMessage({type: 'trackUpdate', modelViewTransform, worldCoords, screenCoords});
+      try {
+        const {worldCoords, screenCoords} = trackingFeatures;
+        this.worker.postMessage({type: 'trackUpdate', modelViewTransform, worldCoords, screenCoords});
+      } catch (err) {
+        this.workerTrackDone = null;
+        resolve(null);
+      }
     });
   }
 
@@ -393,5 +502,6 @@ class Controller {
 }
 
 export {
- Controller
+ Controller,
+ TRACK_EVERY_N_FRAMES
 }
diff --git a/node_modules/mind-ar/src/image-target/input-loader.js b/node_modules/mind-ar/src/image-target/input-loader.js
index 6b58dcf..0d6b2da 100644
--- a/node_modules/mind-ar/src/image-target/input-loader.js
+++ b/node_modules/mind-ar/src/image-target/input-loader.js
@@ -98,6 +98,29 @@ class InputLoader {
     const outInfo = tf.backend().runWebGLProgram(program, inputs, outputType);
     return tf.engine().makeTensorFromDataId(outInfo.dataId, outInfo.shape, outInfo.dtype);
   }
+
+  // Best-effort GPU/canvas release for session teardown (Patch AB).
+  dispose() {
+    try {
+      if (this.tempPixelHandle) {
+        const backend = tf.backend();
+        backend.disposeData(this.tempPixelHandle.dataId);
+        this.tempPixelHandle = null;
+      }
+    } catch (err) {
+      // ignore
+    }
+    try {
+      if (this.context && this.context.canvas) {
+        this.context.canvas.width = 0;
+        this.context.canvas.height = 0;
+      }
+    } catch (err) {
+      // ignore
+    }
+    this.program = null;
+    this.context = null;
+  }
 }
 
 export {
diff --git a/node_modules/mind-ar/src/image-target/three.js b/node_modules/mind-ar/src/image-target/three.js
index aed5e35..7ec0d70 100644
--- a/node_modules/mind-ar/src/image-target/three.js
+++ b/node_modules/mind-ar/src/image-target/three.js
@@ -43,7 +43,8 @@ export class MindARThree {
     this.container.appendChild(this.renderer.domElement);
     this.container.appendChild(this.cssRenderer.domElement);
 
-    window.addEventListener('resize', this.resize.bind(this));
+    this._resizeHandler = this.resize.bind(this);
+    window.addEventListener('resize', this._resizeHandler);
   }
 
   async start() {
@@ -53,12 +54,59 @@ export class MindARThree {
   }
 
   stop() {
-    this.controller.stopProcessVideo();
-    const tracks = this.video.srcObject.getTracks();
-    tracks.forEach(function (track) {
-      track.stop();
-    });
-    this.video.remove();
+    // Remove window.resize before tearing down camera/controller so callbacks
+    // cannot re-enter a half-stopped instance. Idempotent for repeated stop().
+    try {
+      if (this._resizeHandler) {
+        window.removeEventListener('resize', this._resizeHandler);
+        this._resizeHandler = null;
+      }
+    } catch (err) {
+      // Best-effort: do not block camera/tracking cleanup.
+    }
+
+    try {
+      // Patch AB: prefer full controller.dispose() (worker terminate + loader GPU release).
+      if (this.controller) {
+        if (typeof this.controller.dispose === "function") {
+          this.controller.dispose();
+        } else if (typeof this.controller.stopProcessVideo === "function") {
+          this.controller.stopProcessVideo();
+        }
+      }
+    } catch (err) {
+      // Best-effort: continue camera/DOM teardown.
+    }
+
+    // Null-safe camera teardown: video / srcObject / tracks may already be gone.
+    // Stop each MediaStreamTrack at most once; failures must not block remaining cleanup.
+    try {
+      const stream = this.video && this.video.srcObject;
+      const tracks = stream && typeof stream.getTracks === 'function' ? stream.getTracks() : [];
+      const stopped = new Set();
+      for (let i = 0; i < tracks.length; i++) {
+        const track = tracks[i];
+        if (!track || stopped.has(track)) continue;
+        stopped.add(track);
+        try {
+          if (track.readyState !== 'ended' && typeof track.stop === 'function') {
+            track.stop();
+          }
+        } catch (err) {
+          // ignore per-track failures
+        }
+      }
+    } catch (err) {
+      // Best-effort: continue DOM removal.
+    }
+
+    try {
+      if (this.video && typeof this.video.remove === 'function') {
+        this.video.remove();
+      }
+    } catch (err) {
+      // ignore
+    }
   }
 
   switchCamera() {
